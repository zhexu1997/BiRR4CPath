# Copy to .env and source before training:  set -a && source .env && set +a

MODEL_PATH=/path/to/Qwen2.5-VL-7B-Instruct
DATA_DIR=/path/to/json_datasets
MAX_PIXELS=200704

# Optional: open VQA metric
# OPEN_QUESTION_METRIC=bert_score
# MODEL_DIR=/path/to/model_cache
# BERTSCORE_MODEL_ID=roberta-large

# Token-allocation GRPO only
# TASK_PERFORMER_MODEL_PATH=/path/to/merged/task-performer
